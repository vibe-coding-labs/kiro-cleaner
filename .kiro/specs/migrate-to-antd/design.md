# Design Document: Migrate to Ant Design

## Overview

本设计文档描述如何将网站从 Material-UI 迁移到 Ant Design，并重新设计为现代、美观的扁平化风格。Ant Design 以其简洁、优雅的设计语言著称，非常适合扁平化设计。

## Design Philosophy

### 为什么选择 Ant Design？

1. **天生扁平化**: Ant Design 的设计语言本身就是扁平化的
2. **组件丰富**: 提供了丰富的高质量组件
3. **文档完善**: 有详细的中文文档
4. **企业级**: 经过大量企业项目验证
5. **美观**: 默认样式就很美观，不需要过多定制

## Color Palette

### Primary Colors
```typescript
{
  primary: '#1890ff',      // Ant Design 蓝
  success: '#52c41a',      // 成功绿
  warning: '#faad14',      // 警告黄
  error: '#f5222d',        // 错误红
  info: '#1890ff',         // 信息蓝
}
```

### Neutral Colors
```typescript
{
  background: '#ffffff',
  backgroundLight: '#fafafa',
  border: '#d9d9d9',
  textPrimary: 'rgba(0, 0, 0, 0.85)',
  textSecondary: 'rgba(0, 0, 0, 0.65)',
  textDisabled: 'rgba(0, 0, 0, 0.25)',
}
```

## Component Design

### 1. Hero Section
```
┌─────────────────────────────────────────────┐
│  Navigation Bar (fixed top)                 │
├─────────────────────────────────────────────┤
│                                             │
│  [Logo Icon] Kiro Cleaner                   │
│  让 Kiro IDE 轻装上阵                        │
│                                             │
│  清理对话历史、缓存和临时文件                │
│                                             │
│  [立即开始] [了解更多]                       │
│                                             │
│  [Demo Video]                               │
│                                             │
└─────────────────────────────────────────────┘
```

**设计要点:**
- 使用 Ant Design Layout
- 大标题使用 Typography.Title level={1}
- 按钮使用 Button type="primary" 和 type="default"
- 视频容器使用简单的 Card 或 div + border

### 2. Features Section
```
┌──────────┬──────────┐
│ [Icon]   │ [Icon]   │
│ 标题     │ 标题     │
│ 描述     │ 描述     │
├──────────┼──────────┤
│ [Icon]   │ [Icon]   │
│ 标题     │ 标题     │
│ 描述     │ 描述     │
└──────────┴──────────┘
```

**设计要点:**
- 使用 Row + Col 布局
- 每个特性使用 Card 组件
- 图标使用 @ant-design/icons
- hover 时轻微上移 + 边框颜色变化

### 3. Navigation Bar
```
┌─────────────────────────────────────────────┐
│ 🧹 Kiro Cleaner  [特性] [场景] [FAQ] [GitHub]│
└─────────────────────────────────────────────┘
```

**设计要点:**
- 使用 Layout.Header + Menu
- 固定在顶部 (position: fixed)
- 白色背景 + 底部边框
- 移动端使用 Drawer

### 4. How It Works
```
Step 1 → Step 2 → Step 3 → Step 4
[扫描]   [预览]   [清理]   [完成]
```

**设计要点:**
- 使用 Steps 组件（横向）
- 每个步骤使用 Card 展示详情
- 简洁的图标和描述

### 5. Use Cases
```
┌──────────┬──────────┐
│ [Icon]   │ [Icon]   │
│ 场景标题 │ 场景标题 │
│ 描述     │ 描述     │
└──────────┴──────────┘
```

**设计要点:**
- 使用 Card 组件
- 图标使用彩色背景圆形
- 简洁的文字描述

### 6. FAQ
```
▼ 问题 1
  答案 1
▶ 问题 2
▶ 问题 3
```

**设计要点:**
- 使用 Collapse 组件
- 默认展开第一个
- 简洁的问答格式

### 7. Installation
```
[从源码构建] [预编译版本] [使用方法]
─────────────────────────────────
代码块内容
```

**设计要点:**
- 使用 Tabs 组件
- 代码块使用 Typography.Paragraph code
- 简洁的标签页切换

## Theme Configuration

```typescript
import { ConfigProvider } from 'antd';

const theme = {
  token: {
    colorPrimary: '#1890ff',
    borderRadius: 4,
    fontSize: 14,
  },
  components: {
    Button: {
      controlHeight: 40,
      fontSize: 16,
    },
    Card: {
      borderRadiusLG: 8,
    },
  },
};

<ConfigProvider theme={theme}>
  <App />
</ConfigProvider>
```

## Migration Strategy

### Phase 1: Setup (30 min)
1. 安装 Ant Design 和图标库
2. 配置主题
3. 创建新的布局结构

### Phase 2: Core Components (2 hours)
1. 迁移 HeroSection
2. 迁移 Features
3. 迁移 NavigationBar

### Phase 3: Secondary Components (2 hours)
1. 迁移 HowItWorks
2. 迁移 UseCases
3. 迁移 FAQ
4. 迁移 Installation

### Phase 4: Cleanup (1 hour)
1. 移除 Material-UI 依赖
2. 删除未使用的文件
3. 更新测试
4. 优化样式

### Phase 5: Polish (1 hour)
1. 调整间距和对齐
2. 优化响应式布局
3. 添加过渡动画
4. 测试所有功能

## Key Improvements

1. **更简洁的代码**: Ant Design 组件 API 更简洁
2. **更好的默认样式**: 不需要大量自定义样式
3. **更好的响应式**: Grid 系统更灵活
4. **更好的可访问性**: 内置无障碍支持
5. **更快的开发**: 组件即用，减少样式调试时间

## Success Criteria

- ✅ 所有页面使用 Ant Design 组件
- ✅ 完全移除 Material-UI 依赖
- ✅ 界面美观、现代、扁平化
- ✅ 响应式布局在所有设备上正常工作
- ✅ 所有功能正常运行
- ✅ 代码更简洁、易维护
